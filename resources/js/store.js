/**
 * Import vue and vuex.
 */
import Vue from 'vue'
import Vuex from 'vuex'

/**
 * Initialise vuex with vue.
 */
Vue.use( Vuex )

/**
 * Export our dtat store.
 */
export default new Vuex.Store({
    modules: {

    }
});
